name: secure_env_helper_example
description: Minimal Flutter fixture for secure-env-helper manual testing.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_dotenv: ^2.0.0

dev_dependencies:
  build_runner: ^2.12.2
  flutter_lints: ^6.0.0
  flutter_secure_dotenv_generator: ^2.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
